name: Automated Repository Creation Workflow

on:
  issues:
    types: [opened]

permissions:
  issues: write
  contents: write
  pull-requests: write

jobs:
  # ============================================================================
  # Job 1: Validate and Parse Issue
  # ============================================================================
  validate-request:
    name: Validate Repository Request
    runs-on: ubuntu-latest
    if: startsWith(github.event.issue.title, '[REPO REQUEST]')
    outputs:
      repo-name: ${{ steps.parse.outputs.repo-name }}
      tech-stack: ${{ steps.parse.outputs.tech-stack }}
      justification: ${{ steps.parse.outputs.justification }}
      admins: ${{ steps.parse.outputs.admins }}
      default-branch: ${{ steps.parse.outputs.default-branch }}
      defaults: ${{ steps.load-defaults.outputs.defaults }}
      validation-passed: ${{ steps.validate.outputs.passed }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Parse issue body
        id: parse
        uses: actions/github-script@v7
        with:
          script: |
            const issueBody = context.payload.issue.body;

            // Helper function to extract field value
            function extractField(body, fieldLabel) {
              const regex = new RegExp(`### ${fieldLabel}\\s*\\n\\s*([^\\n#]+)`, 'i');
              const match = body.match(regex);
              return match ? match[1].trim() : '';
            }

            // Extract only simplified form fields
            const repoName = extractField(issueBody, 'Repository Name');
            const techStack = extractField(issueBody, 'Tech Stack');
            const techStackOther = extractField(issueBody, 'Other Tech Stack');
            const justification = extractField(issueBody, 'Business Justification');
            const admins = extractField(issueBody, 'Team Maintainers');
            const defaultBranch = extractField(issueBody, 'Default Branch Name');

            // Determine final tech stack
            const finalTechStack = techStack === 'Others (specify below)' ? techStackOther : techStack;

            // Set outputs (only required fields)
            core.setOutput('repo-name', repoName);
            core.setOutput('tech-stack', finalTechStack);
            core.setOutput('justification', justification);
            core.setOutput('admins', admins || context.payload.issue.user.login);
            core.setOutput('default-branch', defaultBranch || 'main');

      - name: Load default values from defaults.yaml
        id: load-defaults
        run: |
          # Install yq if not available
          if ! command -v yq &> /dev/null; then
            wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
            chmod +x /usr/local/bin/yq
          fi

          # Load repository defaults from defaults.yaml
          DEFAULTS=$(yq eval '.repository_defaults' data/defaults.yaml -o json)
          echo "defaults<<EOF" >> $GITHUB_OUTPUT
          echo "$DEFAULTS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Validate repository name
        id: validate-name
        run: |
          REPO_NAME="${{ steps.parse.outputs.repo-name }}"

          # Check if repo name matches pattern (lowercase, hyphens, no spaces)
          if [[ ! "$REPO_NAME" =~ ^[a-z0-9-]+$ ]]; then
            echo "error=Repository name must be lowercase with hyphens only" >> $GITHUB_OUTPUT
            echo "valid=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "valid=true" >> $GITHUB_OUTPUT

      - name: Validate admin usernames
        id: validate-admins
        uses: actions/github-script@v7
        with:
          script: |
            const adminsString = '${{ steps.parse.outputs.admins }}';
            // Filter out empty strings and GitHub's "_No response_" placeholder
            const adminList = adminsString
              .split(',')
              .map(u => u.trim())
              .filter(u => u && u !== '_No response_');

            // Allow empty admin list (requestor will become maintainer)
            if (adminList.length === 0) {
              core.setOutput('valid', 'true');
              core.setOutput('admin-list', JSON.stringify([]));
              core.setOutput('admins-display', context.payload.issue.user.login);
              return;
            }

            const invalidUsers = [];

            // Validate each username exists
            for (const username of adminList) {
              try {
                await github.rest.users.getByUsername({ username });
              } catch (error) {
                invalidUsers.push(username);
              }
            }

            if (invalidUsers.length > 0) {
              core.setOutput('error', `Invalid usernames: ${invalidUsers.join(', ')}`);
              core.setOutput('valid', 'false');
              return;
            }

            core.setOutput('valid', 'true');
            core.setOutput('admin-list', JSON.stringify(adminList));
            core.setOutput('admins-display', adminList.join(', '));

      - name: Check repository existence
        id: check-repo
        uses: actions/github-script@v7
        with:
          script: |
            const repoName = '${{ steps.parse.outputs.repo-name }}';
            try {
              await github.rest.repos.get({
                owner: context.repo.owner,
                repo: repoName
              });
              core.setOutput('exists', 'true');
              core.setOutput('error', `Repository ${repoName} already exists`);
            } catch (error) {
              if (error.status === 404) {
                core.setOutput('exists', 'false');
              } else {
                throw error;
              }
            }

      - name: Consolidate validation
        id: validate
        run: |
          NAME_VALID="${{ steps.validate-name.outputs.valid }}"
          ADMINS_VALID="${{ steps.validate-admins.outputs.valid }}"
          REPO_EXISTS="${{ steps.check-repo.outputs.exists }}"

          if [[ "$NAME_VALID" == "true" && "$ADMINS_VALID" == "true" && "$REPO_EXISTS" != "true" ]]; then
            echo "passed=true" >> $GITHUB_OUTPUT
          else
            echo "passed=false" >> $GITHUB_OUTPUT
          fi

      - name: Generate validation summary
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ steps.validate.outputs.passed }}' === 'true';
            const nameValid = '${{ steps.validate-name.outputs.valid }}' === 'true';
            const adminsValid = '${{ steps.validate-admins.outputs.valid }}' === 'true';
            const repoExists = '${{ steps.check-repo.outputs.exists }}' === 'true';

            let summary = '# 🔍 Repository Request Validation\n\n';

            if (passed) {
              summary += '## ✅ All validations passed!\n\n';
              summary += '| Field | Value |\n';
              summary += '|-------|-------|\n';
              summary += `| **Repository Name** | \`${{ steps.parse.outputs.repo-name }}\` |\n`;
              summary += `| **Tech Stack** | ${{ steps.parse.outputs.tech-stack }} |\n`;
              summary += `| **Team Maintainers** | ${{ steps.validate-admins.outputs.admins-display }} |\n`;
              summary += `| **Default Branch** | ${{ steps.parse.outputs.default-branch }} |\n`;
              summary += `| **Business Justification** | ${{ steps.parse.outputs.justification }} |\n\n`;
              
              summary += '### 👥 Teams to be Created\n\n';
              summary += '| Team Name | Permission | Description |\n';
              summary += '|-----------|------------|-------------|\n';
              summary += `| \`${{ steps.parse.outputs.repo-name }}-dev\` | push | Developer write access |\n`;
              summary += `| \`${{ steps.parse.outputs.repo-name }}-test\` | push | Test manager write access |\n`;
              summary += `| \`${{ steps.parse.outputs.repo-name }}-prod\` | maintain | Production manager maintain access |\n\n`;
              
              summary += '### ⚙️ Default Configuration\n\n';
              summary += '> 📋 All other settings (visibility, features, security, topics, variables) will use organizational defaults from `defaults.yaml`\n\n';
              summary += '---\n\n';
              summary += '### ⏳ Next Steps\n\n';
              summary += '- Awaiting approval from **DevSecOps team**\n';
              summary += '- Once approved, repository and teams will be created automatically\n';
            } else {
              summary += '## ❌ Validation Failed\n\n';
              summary += '### Errors Found:\n\n';
              
              if (!nameValid) {
                summary += `- ❌ **Repository Name:** ${{ steps.validate-name.outputs.error }}\n`;
              }
              if (!adminsValid) {
                summary += `- ❌ **Admin Users:** ${{ steps.validate-admins.outputs.error }}\n`;
              }
              if (repoExists) {
                summary += `- ❌ **Repository Already Exists:** ${{ steps.check-repo.outputs.error }}\n`;
              }
              
              summary += '\n---\n\n';
              summary += '⚠️ **Please fix the above errors and create a new issue.**\n';
            }

            await core.summary
              .addRaw(summary)
              .write();

      - name: Post validation results
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ steps.validate.outputs.passed }}' === 'true';
            const nameValid = '${{ steps.validate-name.outputs.valid }}' === 'true';
            const adminsValid = '${{ steps.validate-admins.outputs.valid }}' === 'true';
            const repoExists = '${{ steps.check-repo.outputs.exists }}' === 'true';

            let commentBody = '## 🔍 Repository Request Validation\n\n';

            if (passed) {
              commentBody += '✅ **All validations passed!**\n\n';
              commentBody += '### Request Summary\n';
              commentBody += `- **Repository Name:** \`${{ steps.parse.outputs.repo-name }}\`\n`;
              commentBody += `- **Tech Stack:** ${{ steps.parse.outputs.tech-stack }}\n`;
              const maintainersText = '${{ steps.validate-admins.outputs.admins-display }}';
              commentBody += `- **Team Maintainers:** ${maintainersText}\n`;
              commentBody += `- **Default Branch:** ${{ steps.parse.outputs.default-branch }}\n\n`;
              
              commentBody += '### Teams to be Created\n';
              commentBody += `- \`${{ steps.parse.outputs.repo-name }}-dev\` (Write permission)\n`;
              commentBody += `- \`${{ steps.parse.outputs.repo-name }}-test\` (Write permission)\n`;
              commentBody += `- \`${{ steps.parse.outputs.repo-name }}-prod\` (Maintain permission)\n\n`;
              
              commentBody += '### Default Configuration\n';
              commentBody += '📋 All other settings (visibility, features, security, topics, variables) will use organizational defaults from the repository template.\n\n';
              
              commentBody += '---\n\n';
              commentBody += '⏳ **Awaiting approval from DevSecOps team...**\n';
              commentBody += 'Once approved, the repository and teams will be created automatically.';
            } else {
              commentBody += '❌ **Validation Failed**\n\n';
              commentBody += '### Errors Found:\n';
              
              if (!nameValid) {
                commentBody += `- ❌ Repository Name: ${{ steps.validate-name.outputs.error }}\n`;
              }
              if (!adminsValid) {
                commentBody += `- ❌ Admin Users: ${{ steps.validate-admins.outputs.error }}\n`;
              }
              if (repoExists) {
                commentBody += `- ❌ Repository Already Exists: ${{ steps.check-repo.outputs.error }}\n`;
              }
              
              commentBody += '\n---\n\n';
              commentBody += '⚠️ **Please fix the above errors and create a new issue.**';
            }

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: commentBody
            });

            // Add label based on validation
            if (passed) {
              await github.rest.issues.addLabels({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                labels: ['validation-passed']
              });
            } else {
              await github.rest.issues.addLabels({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                labels: ['validation-failed']
              });
              
              // Close issue if validation failed
              await github.rest.issues.update({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                state: 'closed'
              });
            }

  # ============================================================================
  # Job 2: Create Repository (Requires Approval)
  # ============================================================================
  create-repository:
    name: Create Repository and Teams
    runs-on: ubuntu-latest
    needs: validate-request
    if: needs.validate-request.outputs.validation-passed == 'true'
    environment: repo-creation-approval # Requires approval from paloitmbb-devsecops

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Update repositories.yaml
        id: update-repos
        run: |
          # Install yq if not available
          if ! command -v yq &> /dev/null; then
            sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
            sudo chmod +x /usr/local/bin/yq
          fi

          # Load defaults from defaults.yaml
          DEFAULTS=$(yq eval '.repository_defaults' data/defaults.yaml -o json)

          # Extract default values
          VISIBILITY=$(echo "$DEFAULTS" | jq -r '.visibility')
          HAS_ISSUES=$(echo "$DEFAULTS" | jq -r '.features.has_issues')
          HAS_PROJECTS=$(echo "$DEFAULTS" | jq -r '.features.has_projects')
          HAS_WIKI=$(echo "$DEFAULTS" | jq -r '.features.has_wiki')

          # Security settings
          VULN_ALERTS=$(echo "$DEFAULTS" | jq -r '.security.enable_vulnerability_alerts')
          ADV_SECURITY=$(echo "$DEFAULTS" | jq -r '.security.enable_advanced_security')
          SECRET_SCAN=$(echo "$DEFAULTS" | jq -r '.security.enable_secret_scanning')
          SECRET_PUSH=$(echo "$DEFAULTS" | jq -r '.security.enable_secret_scanning_push_protection')
          DEPENDABOT_ALERTS=$(echo "$DEFAULTS" | jq -r '.security.enable_dependabot_alerts')
          DEPENDABOT_UPDATES=$(echo "$DEFAULTS" | jq -r '.security.enable_dependabot_security_updates')

          # Get topics from default and add tech stack
          DEFAULT_TOPICS=$(echo "$DEFAULTS" | jq -r '.topics | join(", ")')
          TECH_STACK="${{ needs.validate-request.outputs.tech-stack }}"
          TECH_TOPIC=$(echo "$TECH_STACK" | tr '[:upper:]' '[:lower:]' | tr ' ' '-')

          # Build topics array (defaults + tech stack)
          TOPICS_ARRAY="[]"
          if [ -n "$DEFAULT_TOPICS" ]; then
            # Start with default topics
            IFS=',' read -ra TOPIC_ITEMS <<< "$DEFAULT_TOPICS"
            TOPICS_ARRAY="["
            for topic in "${TOPIC_ITEMS[@]}"; do
              topic=$(echo "$topic" | xargs)  # trim whitespace
              TOPICS_ARRAY="${TOPICS_ARRAY}\"${topic}\", "
            done
            # Add tech stack
            TOPICS_ARRAY="${TOPICS_ARRAY}\"${TECH_TOPIC}\"]"
          else
            TOPICS_ARRAY="[\"${TECH_TOPIC}\"]"
          fi

          # Generate description
          REPO_NAME="${{ needs.validate-request.outputs.repo-name }}"
          DESCRIPTION=$(echo "$REPO_NAME" | sed 's/-/ /g' | awk '{for(i=1;i<=NF;i++) $i=toupper(substr($i,1,1)) tolower(substr($i,2));}1')
          DESCRIPTION="${DESCRIPTION} using ${TECH_STACK}"

          # Append to repositories.yaml
          cat >> data/repositories.yaml << EOF

            - name: ${REPO_NAME}
              description: "${DESCRIPTION}"
              visibility: ${VISIBILITY}
              features:
                has_issues: ${HAS_ISSUES}
                has_projects: ${HAS_PROJECTS}
                has_wiki: ${HAS_WIKI}
              default_branch: ${{ needs.validate-request.outputs.default-branch }}
              topics: ${TOPICS_ARRAY}
              security:
                enable_vulnerability_alerts: ${VULN_ALERTS}
                enable_advanced_security: ${ADV_SECURITY}
                enable_secret_scanning: ${SECRET_SCAN}
                enable_secret_scanning_push_protection: ${SECRET_PUSH}
                enable_dependabot_alerts: ${DEPENDABOT_ALERTS}
                enable_dependabot_security_updates: ${DEPENDABOT_UPDATES}
          EOF

          echo "Successfully updated repositories.yaml with defaults"

      - name: Update teams.yaml
        id: update-teams
        run: |
          REPO_NAME="${{ needs.validate-request.outputs.repo-name }}"
          TEAMS_FILE="data/teams.yaml"

          # Append 3 teams for the new repository
          cat >> "$TEAMS_FILE" << EOF

            - name: ${REPO_NAME}-dev
              repository: ${REPO_NAME}
              permission: push
              privacy: closed
              description: "Developer team for ${REPO_NAME} repository - write access for development"
            
            - name: ${REPO_NAME}-test
              repository: ${REPO_NAME}
              permission: push
              privacy: closed
              description: "Test team for ${REPO_NAME} repository - write access for testing activities"
            
            - name: ${REPO_NAME}-prod
              repository: ${REPO_NAME}
              permission: maintain
              privacy: closed
              description: "Production team for ${REPO_NAME} repository - maintain access for production releases"
          EOF

          echo "Successfully updated teams.yaml"

      - name: Create branch and commit changes
        id: create-branch
        run: |
          REPO_NAME="${{ needs.validate-request.outputs.repo-name }}"
          BRANCH_NAME="repo-request/${REPO_NAME}"
          
          git checkout -b "${BRANCH_NAME}"
          git add data/repositories.yaml data/teams.yaml
          git commit -m "feat: ✨ add repository ${REPO_NAME} and associated teams

          Repository request from issue #${{ github.event.issue.number }}

          - Repository: ${REPO_NAME}
          - Tech Stack: ${{ needs.validate-request.outputs.tech-stack }}
          - Default Branch: ${{ needs.validate-request.outputs.default-branch }}
          - Configuration: Using organizational defaults
          - Created 3 teams: dev, test, prod

          Related to #${{ github.event.issue.number }}"
          
          git push origin "${BRANCH_NAME}"
          
          echo "branch=${BRANCH_NAME}" >> $GITHUB_OUTPUT

      - name: Create Pull Request
        id: create-pr
        uses: actions/github-script@v7
        with:
          script: |
            const repoName = '${{ needs.validate-request.outputs.repo-name }}';
            const techStack = '${{ needs.validate-request.outputs.tech-stack }}';
            const defaultBranch = '${{ needs.validate-request.outputs.default-branch }}';
            const branchName = '${{ steps.create-branch.outputs.branch }}';
            const issueNumber = context.issue.number;
            
            const admins = '${{ needs.validate-request.outputs.admins }}';
            const adminsList = admins || context.payload.issue.user.login;
            
            const prBody = `## 🎉 New Repository Request

### Repository Details

| Field | Value |
|-------|-------|
| **Repository Name** | \`${repoName}\` |
| **Tech Stack** | ${techStack} |
| **Default Branch** | \`${defaultBranch}\` |
| **Admin Users** | ${adminsList} |
| **Requested By** | @${context.payload.issue.user.login} |

### Teams Created

- \`${repoName}-dev\` - Developer write access
- \`${repoName}-test\` - Test manager write access
- \`${repoName}-prod\` - Production manager maintain access

### Configuration

This PR adds the repository definition and team configurations to:
- \`data/repositories.yaml\`
- \`data/teams.yaml\`

**Default settings** from \`data/defaults.yaml\` will be applied:
- Visibility: private
- Features: Issues, Projects (Wiki disabled)
- Security: Vulnerability alerts, Dependabot enabled
- Topics: maybank, mbb, ${techStack.toLowerCase().replace(/ /g, '-')}

### Next Steps

1. ✅ Review the YAML changes
2. ✅ Merge this PR to apply changes
3. 🚀 Run Terraform to create the repository and teams

---

Closes #${issueNumber}`;
            
            const pr = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `🆕 Add repository: ${repoName}`,
              head: branchName,
              base: 'main',
              body: prBody
            });
            
            core.setOutput('pr-number', pr.data.number);
            core.setOutput('pr-url', pr.data.html_url);
            return pr.data;

      - name: Add labels to PR
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.create-pr.outputs.pr-number }},
              labels: ['repo-request', 'automated']
            });

      - name: Prepare team maintainers info
        id: prepare-maintainers
        uses: actions/github-script@v7
        with:
          script: |
            const adminsString = '${{ needs.validate-request.outputs.admins }}';
            let maintainerList = adminsString.split(',').map(u => u.trim()).filter(u => u);

            // If no admins specified, use issue creator
            if (maintainerList.length === 0) {
              maintainerList = [context.payload.issue.user.login];
            }

            const repoName = '${{ needs.validate-request.outputs.repo-name }}';
            const teams = ['dev', 'test', 'prod'];
            const results = [];

            for (const teamSuffix of teams) {
              const teamSlug = `${repoName}-${teamSuffix}`;
              for (const username of maintainerList) {
                results.push(`📋 ${username} will be maintainer of ${teamSlug}`);
              }
            }

            core.setOutput('results', results.join('\n'));
            core.setOutput('maintainers', maintainerList.join(', '));
            return results;

- name: Generate PR creation summary
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const repoName = '${{ needs.validate-request.outputs.repo-name }}';
            const techStack = '${{ needs.validate-request.outputs.tech-stack }}';
            const defaultBranch = '${{ needs.validate-request.outputs.default-branch }}';
            const maintainers = '${{ steps.prepare-maintainers.outputs.maintainers }}';
            const prUrl = '${{ steps.create-pr.outputs.pr-url }}';
            const prNumber = '${{ steps.create-pr.outputs.pr-number }}';
            
            let summary = '# ✅ Pull Request Created Successfully!\n\n';
            summary += `## 🎉 Repository Configuration Ready\n\n`;
            summary += '| Field | Value |\n';
            summary += '|-------|-------|\n';
            summary += `| **Repository Name** | \`${repoName}\` |\n`;
            summary += `| **Tech Stack** | ${techStack} |\n`;
            summary += `| **Default Branch** | \`${defaultBranch}\` |\n`;
            summary += `| **Team Maintainers** | ${maintainers} |\n`;
            summary += `| **Pull Request** | [#${prNumber}](${prUrl}) |\n\n`;
            
            summary += '## 👥 Teams to be Created\n\n';
            summary += '| Team | Permission | Description |\n';
            summary += '|------|------------|-------------|\n';
            summary += `| \`${repoName}-dev\` | push | Developer write access |\n`;
            summary += `| \`${repoName}-test\` | push | Test manager write access |\n`;
            summary += `| \`${repoName}-prod\` | maintain | Production manager maintain access |\n\n`;
            
            summary += '## 📋 Next Steps\n\n';
            summary += `1. 📝 Review the PR: [#${prNumber}](${prUrl})\n`;
            summary += `2. ✅ Approve and merge the PR\n`;
            summary += `3. 🚀 Run Terraform apply to create repository and teams\n`;
            summary += `4. 👥 Assign team maintainers via GitHub UI\n\n`;
            
            summary += '---\n\n';
            summary += `*PR created for issue #${context.issue.number}*\n`;

            await core.summary
              .addRaw(summary)
              .write();

      - name: Post success comment
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const repoName = '${{ needs.validate-request.outputs.repo-name }}';
            const prUrl = '${{ steps.create-pr.outputs.pr-url }}';
            const prNumber = '${{ steps.create-pr.outputs.pr-number }}';

            const commentBody = `## ✅ Pull Request Created!

            A pull request has been created to add your repository configuration.

            ### 🎉 Configuration Details
            - **Repository Name:** \`${repoName}\`
            - **Tech Stack:** ${{ needs.validate-request.outputs.tech-stack }}
            - **Default Branch:** ${{ needs.validate-request.outputs.default-branch }}
            - **Pull Request:** [#${prNumber}](${prUrl})

            ### 👥 Teams to be Created
            - \`${repoName}-dev\` - Developer write access
            - \`${repoName}-test\` - Test manager write access
            - \`${repoName}-prod\` - Production manager maintain access

            ### 👤 Team Maintainers
            ${{ steps.prepare-maintainers.outputs.results }}

            ### 📋 Next Steps
            1. 📝 Review the pull request: [#${prNumber}](${prUrl})
            2. ✅ Approve and merge the PR
            3. 🚀 Run \`terraform apply\` to create the repository and teams
            4. 👥 Assign team maintainers via GitHub UI

            ---
            *Automated by GitHub Actions workflow*`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: commentBody
            });

            // Link issue to PR (will be closed when PR is merged)
            await github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['pr-created']
            });

      - name: Generate failure summary
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const repoName = '${{ needs.validate-request.outputs.repo-name }}';

            let summary = '# ❌ Repository Creation Failed\n\n';
            summary += `## 🚨 Error Information\n\n`;
            summary += '| Field | Value |\n';
            summary += '|-------|-------|\n';
            summary += `| **Repository Name** | \`${repoName}\` |\n`;
            summary += `| **Workflow Run** | [View Logs](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}) |\n`;
            summary += `| **Issue** | #${context.issue.number} |\n\n`;

            summary += '## 🔧 Troubleshooting Steps\n\n';
            summary += '1. 📋 Check the workflow logs for detailed error messages\n';
            summary += '2. 🔍 Verify that all required permissions are in place\n';
            summary += '3. 🔄 Ensure no manual changes conflict with automation\n';
            summary += '4. 👥 Contact DevSecOps team for assistance\n\n';

            summary += '---\n\n';
            summary += '> ⚠️ A member of the DevSecOps team will investigate and follow up.\n';

            await core.summary
              .addRaw(summary)
              .write();

      - name: Post failure comment
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const commentBody = `## ❌ Repository Creation Failed

            There was an error creating the repository. Please check the workflow logs for details.

            ### Error Details
            - **Workflow Run:** [View Logs](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})
            - **Step Failed:** Check the logs above

            ### 🔧 Troubleshooting
            A member of the DevSecOps team will investigate and follow up.

            ---
            *Automated by GitHub Actions workflow*`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: commentBody
            });

            // Add failure label
            await github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['creation-failed']
            });
