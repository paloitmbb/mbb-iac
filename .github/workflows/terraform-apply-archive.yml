
# =============================================================================
# ONE-TIME WORKFLOW: Clean up orphaned security state for archived repos,
# then run terraform apply. Trigger manually via workflow_dispatch, verify
# success, then DELETE this file.
# =============================================================================
name: "One-Time: Terraform Apply Archive Cleanup"

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to apply"
        required: true
        type: choice
        options:
          - dev
          - production

permissions:
  contents: write
  issues: write
  actions: write
  id-token: write  # Required for OIDC authentication with Azure

jobs:
  terraform-apply:
    name: 🧹 One-Time Archive Cleanup & Apply
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: ${{ fromJSON(format('["{0}"]', inputs.environment)) }}

    environment:
      name: ${{ matrix.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login with OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ARM_CLIENT_ID }}
          tenant-id: ${{ secrets.ARM_TENANT_ID }}
          subscription-id: ${{ secrets.ARM_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.5
          terraform_wrapper: false

      - name: Terraform Init
        run: |
          terraform init -backend-config=environments/${{ matrix.environment }}/backend.tfvars
        env:
          GITHUB_TOKEN: ${{ secrets.ORG_GITHUB_TOKEN }}
          # Azure authentication via OIDC (azure/login@v2 handles token)
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
          ARM_USE_OIDC: true

      - name: Clean up security state for archived repos
        run: |
          # When a repo is archived, its github_security module instance must be
          # removed from state first — the GitHub API rejects changes on archived
          # repos, so Terraform cannot destroy the dependabot resource normally.
          echo "🔍 Checking for orphaned security module instances..."
          STATE_LIST=$(terraform state list 2>/dev/null || echo "")
          CLEANED=0

          for item in $STATE_LIST; do
            repo_key=$(echo "$item" | sed -n 's/^module\.github_security\["\([^"]*\)"\].*/\1/p')
            [ -z "$repo_key" ] && continue

            # Deduplicate — multiple resources per module instance
            if echo "$SEEN_KEYS" | grep -qF "|${repo_key}|" 2>/dev/null; then
              continue
            fi
            SEEN_KEYS="${SEEN_KEYS}|${repo_key}|"

            is_archived=$(python3 -c "
          import yaml, sys
          with open('data/repositories.yaml') as f:
              data = yaml.safe_load(f)
          for repo in data.get('repositories', []):
              if repo['name'] == sys.argv[1] and repo.get('archived', False):
                  print('true')
                  sys.exit(0)
          print('false')
          " "$repo_key" 2>/dev/null || echo "false")

            if [ "$is_archived" = "true" ]; then
              echo "🗑️  Removing security state for archived repo: $repo_key"
              terraform state rm "module.github_security[\"$repo_key\"]" || true
              CLEANED=$((CLEANED + 1))
            fi
          done

          if [ "$CLEANED" -gt 0 ]; then
            echo "✅ Cleaned $CLEANED orphaned security module instance(s)"
          else
            echo "✅ No orphaned security state found"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.ORG_GITHUB_TOKEN }}
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
          ARM_USE_OIDC: true

      - name: Terraform Plan
        run: |
          terraform plan -var-file=environments/${{ matrix.environment }}/terraform.tfvars -out=environments/${{ matrix.environment }}/tfplan
        env:
          GITHUB_TOKEN: ${{ secrets.ORG_GITHUB_TOKEN }}
          # Azure authentication via OIDC (azure/login@v2 handles token)
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
          ARM_USE_OIDC: true

      - name: Terraform Apply
        run: |
          terraform apply -auto-approve environments/${{ matrix.environment }}/tfplan
        env:
          GITHUB_TOKEN: ${{ secrets.ORG_GITHUB_TOKEN }}
          # Azure authentication via OIDC (azure/login@v2 handles token)
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
          ARM_USE_OIDC: true
